# 下一阶段开发计划（OMC / OMA）

对照 OpenOcta Server 管理端开局后的可做项，拆成两份独立报告。

| 报告        | 仓             | 文件                                 |
| ----------- | -------------- | ------------------------------------ |
| OnMyCompany | 本仓           | [OMC-DEV-PLAN.md](./OMC-DEV-PLAN.md) |
| OnMyAgent   | `../onmyagent` | [OMA-DEV-PLAN.md](./OMA-DEV-PLAN.md) |

目录用 `docs/plan/`（本仓是 `docs/`，没有 `doc/`）。产品 SoT 仍在 [../onmycompany/](../onmycompany/README.md)；这里只放「下一轮要开发什么」。

契约变更仍先改 `DESKTOP-CONTRACT` + `CONFIG-SCHEMA` + `API-NOTES`，再动两端代码。
